defmodule TapestryDos.RoutingTables do

    # def build_routing_tables (num_nodes) do
    #     #Take SHA of i compare it with every j and arrange accordingly
    #     nodes_list = 1..num_nodes
    #     actual_nodes_list = Enum.to_list(nodes_list)
    #     Enum.each( actual_nodes_list, 
    #     fn x -> make_routing_table(x)
    #     end)
        
    # end
    
    # def make_routing_table (node) do
    #     Enum.each(1..40 fn i-> find_column(i,node) end)
    # end

    # def find_column (j) do
    #     Enum.each(1..16 fn -> find_column(i,node))
    #     compare_node_with_each(i,node)
    # end

    # def candidates_in_block(block_num) do
       
    # end

    # def hash_of(number) do
    #     hex_val = :crypto.hash(:sha, number |> Integer.to_string ) |> Base.encode16
    #     hex_val
    # end

    # def match
end